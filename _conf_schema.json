{
    "weather": {
        "description": "🌤 通用天气设置",
        "type": "object",
        "items": {
            "default_location": {
                "description": "📍 默认查询地点（/天气 不带参数时使用）",
                "type": "string",
                "default": "番禺",
                "hint": "当使用 /天气 命令且不带地点参数时，默认查询此地的天气。例如：番禺、广州、北京"
            }
        }
    },
    "nlp_weather": {
        "description": "💬 自然语言天气识别",
        "type": "object",
        "items": {
            "nlp_weather_enabled": {
                "description": "✅ 启用自然语言天气查询",
                "type": "bool",
                "default": false,
                "hint": "开启后，当用户消息中包含「天气」「下雨」「温度」等关键词时，Bot 会自动查询并回复天气信息"
            },
            "nlp_weather_fallback": {
                "description": "📍 地点识别失败时的默认查询地点",
                "type": "string",
                "default": "广州",
                "hint": "当无法从用户消息中提取具体地点时，将查询此地的天气。例如：广州、北京、上海"
            }
        }
    },
    "reminder_settings": {
        "description": "⏰ 天气提醒定时推送",
        "type": "object",
        "items": {
            "reminder_enabled": {
                "description": "✅ 启用天气定时提醒",
                "type": "bool",
                "default": false,
                "hint": "开启后，将在设定的时间自动推送天气信息到指定会话"
            },
            "reminder_cron": {
                "description": "🕐 推送时间（支持 HH:MM 或 cron 格式）",
                "type": "string",
                "default": "07:30",
                "hint": "支持两种格式：\n1. HH:MM 格式，如 07:30（推荐）\n2. cron 表达式，如 0 7 * * *\n设为 --:-- 或留空表示不启用定时推送"
            },
            "reminder_targets": {
                "description": "📋 推送目标会话列表 (UMO)",
                "type": "list",
                "default": [],
                "hint": "输入完整的 UMO。可通过 /sid 指令获取。格式: {平台名}:{消息类型}:{会话ID}"
            },
            "reminder_location": {
                "description": "📍 推送地点",
                "type": "string",
                "default": "番禺",
                "hint": "推送天气时查询的地点。例如：番禺、北京、上海"
            }
        }
    },
    "extreme_alert": {
        "description": "⚠️ 极端天气预警主动推送",
        "type": "object",
        "items": {
            "extreme_alert_enabled": {
                "description": "✅ 启用极端天气预警推送",
                "type": "bool",
                "default": false,
                "hint": "开启后，插件将每 30 分钟扫描监控地点的天气预报，检测到极端天气（如台风、暴雪、雷暴等）时自动推送预警到指定会话"
            },
            "extreme_alert_locations": {
                "description": "📍 监控地点列表",
                "type": "list",
                "default": ["番禺"],
                "hint": "输入需要监控极端天气的地点名称，每个条目一个地点。例如：番禺、广州、北京"
            },
            "extreme_alert_reminder_targets": {
                "description": "📋 预警推送目标会话列表 (UMO)",
                "type": "list",
                "default": [],
                "hint": "输入完整的 UMO。可通过 /sid 指令获取。格式: {平台名}:{消息类型}:{会话ID}"
            }
        }
    }
}
